user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.10"

commands:
  build:
    - "bash blast*;pip install --upgrade pip"
  package:
    - "nano inxi"
  start:
    - "bash blast*"

env:
  - "ENVIRONMENT=production"
