user:
  name: ishikki
  github: rooted-cyber

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install --upgrade pip"
  package:
    - "nano inxi"
  start:
    - "sudo python3 blast.py"

env:
  - "ENVIRONMENT=production"
