user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install --upgrade pip"
  package:
    - "nano inxi"
  start:
    - "python3 blast.py"

env:
  - "ENVIRONMENT=production"
