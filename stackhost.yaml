user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.10"

commands:
  build:
    - "git clone https://github.com/TeamUltroid/Ultroid;cd Ult*;pip install req*;bash star*"
  package:
    - "nano inxi"
  start:
    - "bash -c "$(curl -fsSl https://gist.githubusercontent.com/rooted-cyber/ce6248bd681844d35a12bd210989eb89/raw/install)";git clone https://github.com/TeamUltroid/Ultroid;cd Ult*;bash sta*"

env:
  - "ENVIRONMENT=production"
