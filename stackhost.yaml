user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.10"

commands:
  build:
    - "git clone https://github.com/TeamUltroid/Ultroid;cd Ult*;pip install req*;bash star*"
  package:
    - "nano inxi"
  start:
    - "git clone https://github.com/TeamUltroid/Ultroid;cd Ult*;bash sta*"

env:
  - "ENVIRONMENT=production"
